// The extension-point name for Mobile Bar cells, on its own so the editor can
// name it without importing the resolver beside it - lib/puck/mobile-bar-items.ts
// reaches the database, and lib/puck/config.core.tsx is client-safe by contract.
export const MOBILE_BAR_POINT = 'core.mobile-bar-items'
